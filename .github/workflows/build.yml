# Automatically build the project and run any configured tests for every push
# and submitted pull request. This can help catch issues that only occur on
# certain platforms or Java versions, and provides a first line of defence
# against bad commits.

name: build
on: [pull_request, push]

jobs:
  build:
    runs-on: windows-latest

    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout del código
        uses: actions/checkout@v2

      - name: Configurar Java 17
        uses: actions/setup-java@v2
        with:
          distribution: "adopt"
          java-version: "17"

      - name: Configuración de Java y construcción con Gradle
        run: |
          # Construir con Gradle
          ./gradlew.bat build

      - name: Crear nueva versión en GitHub y Subir JAR como artefacto
        run: |
          mkdir artifacts
          cp build/libs/brixpansion-refabricated-1.0.1.jar artifacts/
          
          # Configurar git para el cli de GitHub
          gh auth login --with-token $GH_TOKEN

          # Crear nueva versión en GitHub y subir el JAR como asset
          gh release create v1.0.1 $(find artifacts -type f)

      - name: Subir JAR como artefacto
        uses: actions/upload-artifact@v2
        with:
          name: brixpansion-refabricated-1.0.1.jar
          path: artifacts/